{
  "name": "deploy-test",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build needed for static HTML'",
    "start": "npx serve . -p 3000"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
